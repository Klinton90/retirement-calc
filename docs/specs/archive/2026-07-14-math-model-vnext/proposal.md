# Proposal: Math model vNext

Implement bucket-aware target engine, RRIF rules, correct pension/tax character, conversion recommendation, and contribution policy per `docs/explanation/math-model.md`.

## Scope

- Phases A, B1, B2, C, D (E UI polish only as needed to surface new outputs)
- UI may change to show new cards; not a visual redesign pass

## Out of scope

- Full OpenSpec CLI, Monte-Carlo, full non-reg CG precision, FX, partial RRIF convert (v2)
